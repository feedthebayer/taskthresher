<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Your to-do list has become unwieldy. You need something that actually works. Meet TaskThresher, an awesome little application by Brandon Bayer.">

    <title><%= content_for?(:title) ? yield(:title) : "TaskThresher | Created by Brandon Bayer" %></title>

    <script src="//use.typekit.net/stn2owb.js"></script>
    <script>try{Typekit.load();}catch(e){}</script>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application" 'data-turbolinks-track' => true %>
    <%= javascript_include_tag :application %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header id="header-wrapper">
      <section class="content">
        <h1 class="logo">
          <%= link_to root_path do %>
            <span class="first">Task</span><span class="second">Thresher</span>
          <% end %>
        </h1>

        <nav>
          <% if signed_in? %>
            <%= button_to 'Sign Out', sign_out_path, method: :delete %>
          <% elsif @sign_in_page %>
            <%= link_to 'Register', register_path, class: "register" %>
          <% else %>
            <%= link_to 'Sign In', sign_in_path %>
          <% end %>
        </nav>
      </section>

      <section id="flash">
        <% flash.each do |name, msg| -%>
          <% if msg.is_a?(String) %>
            <!-- <div data&#45;alert
            class="alert-box <%= name.to_s == 'notice' ? 'success' : 'alert' %>"> -->
            <div data-alert class="alert-box alert">
              <%= msg %>
              <!-- <a href="#" class="close">&#38;times;</a> -->
            </div>
          <% end %>
        <% end %>
      </section>
    </header>

    <%= yield %>

  </body>
</html>
